#include<stdio.h>
#include<conio.h>
int main()
{
	char one[20];
	char two[20];
	printf("enter the two strings");
	scanf("%s%s",&one,&two);
	printf("the concatenate of two strings are %s%s",one,two);
	getch();
	return 0;
}